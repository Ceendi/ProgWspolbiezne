<UserControl x:Class="ProgWspolbiezne.View.CreateBallsMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgWspolbiezne.View"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1000">
    <Grid>
		<Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       FontSize="30"
                       Text="Podaj Liczbe Kul"/>
            <TextBox MaxLength="2" PreviewTextInput="NumValTextBox" Grid.Row="1" FontSize="30" Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged}" Margin="0 10 10 10" />
        </Grid>
        <StackPanel  Grid.Row="1" Orientation="Horizontal" >
            <Button Content="Start" Command="{Binding StartCommand}"></Button>
            <Button Command="{Binding ExitCommand}" Margin="10 0 0 0" Content="Exit"></Button>
        </StackPanel>
    </Grid>
</UserControl>
